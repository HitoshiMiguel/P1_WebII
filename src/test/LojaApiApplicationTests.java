package com.loja.loja_api;  // Pacote correto

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = LojaApiApplication.class)
public class LojaApiApplicationTests {

    @Test
    void contextLoads() {
        // Teste para garantir que o contexto do Spring foi carregado
    }
}
